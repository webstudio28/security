<section class="relative overflow-hidden section-pad text-white">
  <div class="absolute inset-0 bg-gradient-to-br from-brand-deeper via-[#16191d] to-brand-mist" aria-hidden="true"></div>
  <div
    class="absolute inset-0 scale-[1.02] bg-cover bg-center opacity-10"
    style="background-image:url('{{ '/assets/images/process-bg.jpg' | url }}')"
    aria-hidden="true"
  ></div>

  <div class="container-site relative z-10">
    <div class="reveal">
      <div class="eyebrow text-brand before:bg-brand">Как работим</div>
      <h2 class="section-title text-ink">От запитването до активна охрана</h2>
      <p class="section-copy text-white/65">Ясен процес, точна комуникация и професионално изпълнение.</p>
    </div>
    <div class="mt-12 grid gap-10 sm:grid-cols-2 lg:grid-cols-4">
      {% for step in home.process %}
        <article class="reveal relative">
          <span class="relative z-[1] mb-6 grid h-14 w-14 place-items-center rounded-full border border-brand/65 bg-brand-deeper/80 font-display text-[17px] font-extrabold text-brand">0{{ loop.index }}</span>
          <h3 class="mb-2.5 font-display text-[22px] font-extrabold">{{ step.title }}</h3>
          <p class="m-0 text-[15px] leading-relaxed text-white/60">{{ step.text }}</p>
        </article>
      {% endfor %}
    </div>
    <div class="mt-10 reveal">
      <button class="btn btn-primary open-inquiry" type="button">Заяви охрана</button>
    </div>
  </div>
</section>
